import { NativeStackScreenProps } from '@react-navigation/native-stack';
import { RootStackParamList } from '../../navigation/types';

export type TTopicsScreenPropstype = NativeStackScreenProps<
  RootStackParamList,
  'TopicsScreen'
>;
